package main

import (
	"menu"
)

func main() {

	// userList := new(nodes.Node)

	menu.Menu()

	// start := fun.NewNode(nodes.NewStudent(1, "Juan Pablo", "contrasegna"))

	// start = fun.AddStart(start, nodes.NewStudent(2, "Juan Pablo", "contrasegna"))
	// start = fun.AddStart(start, nodes.NewStudent(3, "Juan Pablo", "contrasegna"))
	// start = fun.AddStart(start, nodes.NewStudent(4, "Juan Pablo", "contrasegna"))

	// start = fun.AddStart(start, nodes.NewStudent(5, "Juan Pablo", "contrasegna"))

	// start = fun.AddEnd(start, nodes.NewStudent(6, "Juan Pablo", "contrasegna"))
	// start = fun.AddEnd(start, nodes.NewStudent(7, "Juan Pablo", "contrasegna"))
	// start = fun.AddEnd(start, nodes.NewStudent(8, "Juan Pablo", "contrasegna"))
	// start = fun.AddEnd(start, nodes.NewStudent(9, "Juan Pablo", "contrasegna"))

	// start = fun.RemoveStart(start)
	// start = fun.RemoveEnd(start)

	// fun.PrintAll(start)

}
